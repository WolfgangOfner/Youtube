apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aksdemoapp.fullname" . }}-test-connection"
  labels:
    {{- include "aksdemoapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aksdemoapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
