apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "newingressdemo.fullname" . }}-test-connection"
  labels:
    {{- include "newingressdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "newingressdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
