apiVersion: v1
kind: Service
metadata:
  name: {{ include "newingressdemo.fullname" . }}
  labels:
    {{- include "newingressdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newingressdemo.selectorLabels" . | nindent 4 }}
